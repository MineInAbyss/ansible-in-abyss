# Generate from cloudflare's dashboard, used for ssl certificates
cloudflare_email: "email"
cloudflare_api_key: "key"

# Users to be given root access on the machine
ssh_users:
  - name: "name"
    ssh_public_key: ...
  - ...
oauth2_proxy:
  client_id: ...
  client_secret: ...
